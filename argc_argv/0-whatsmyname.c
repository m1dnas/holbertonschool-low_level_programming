#include <stdio.h>
/**
 * main - prints its name
 * @argc: argc
 * @argv: argv
 * Return: 0 if it's ok
 */
int main(__attribute__((unused)) int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
