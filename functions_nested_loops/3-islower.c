#include "main.h"
#include <ctype.h>

/**
 * _islower - checks for lowercase character
 * @c: parameter to check
 * Return: 1 if c is lowercase, 0 otherwise
 **/
int _islower(int c)
{
	int c;

	_islower(c);
	return (0);
}
