#include <stdio.h>

/**
 *  * main - test
 *   * Return: its OK
 *    **/
int main(void)
{
	int x;

	for (x = 48 ; x <= 57 ; x++)
	{
	putchar(n);
	if (n < 57)
	{
	putchar(',');
	putchar('\n');
	}
	}
	putchar('\n');
	return (0);
}
