#include <stdio.h>

/**
 * main - prueba123
 * Return: its OK
 **/
int main(void)
{
	char ch;

	for (ch = 'a' ; ch <= 'Z' ; ch++)
{
	putchar(ch);
}
	char c;

	for (c = 'A' ; c <= 'Z' ; c++)
{
	putchar(c);
}
return (0);
}
