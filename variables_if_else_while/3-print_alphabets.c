#include <stdio.h>

/**
 * main - prueba123
 * Return: its OK
 **/
int main(void)
{
	char ch;

	for (ch = 'a' ; ch <= 'Z' ; ch++)
{
	putchar(ch);
}
	for (ch = 'A' ; ch <= 'Z' ; ch++)
{
	putchar(ch);
}
return (0);
}
