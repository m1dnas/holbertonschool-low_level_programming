#include <stdio.h>

/**
 * main - prueba123
 * Return: its OK
 **/
int main(void)
{
	char ch;
	char c;

	for (ch = 'a' ; ch <= 'Z' ; ch++)
{
	putchar(ch);
}
	for (c = 'A' ; c <= 'Z' ; c++)
{
	putchar(c);
}
return (0);
}
