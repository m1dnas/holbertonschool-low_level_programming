#include "dog.h"
#include <stdlib.h>

/**
 * print_dog - prints a struct dog
 * @d: d
 * Return: void
 **/
void print_dog(struct dog *d)
{
	if (d == NULL)
	{
	}
	if (name == NULL)
	{
		printf("Name: (nil)");
	}
}
