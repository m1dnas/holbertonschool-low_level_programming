#include "main.h"
#include <stdio.h>

/**
 * mul - function that multiples two integers
 * @a: parameter that multiples
 * @b: parameter that multipless
 * Return: 0
 **/
int mul(int a, int b)
{
	return (a * b);
}
