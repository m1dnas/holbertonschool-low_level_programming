#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * print_rev - function that prints a string, in reverse
 * @s: the string
 * Return: void
 **/
void print_rev(char *s)
{
	int a, b, len;
	a = 0;
	
	while (a != '\0')
	{
		a++;
	}
	len = a

	for (b = len; b > 0; b--)
	{
		putchar(s[b]);
	}
}
