#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - function that returns the length of a string
 * @s: the string
 * Return: the lenght of a string
 **/
int _strlen(char *s)
{
	return (strlen(s));
}
